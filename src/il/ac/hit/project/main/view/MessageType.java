package il.ac.hit.project.main.view;

/**
 * Represents the type of message to be displayed to the user.
 * <p>
 * This enum is used to categorize messages into different types, such as success, error,
 * information, and warning, allowing the UI to render them appropriately.
 */
public enum MessageType {
    SUCCESS,
    ERROR,
    INFO,
    WARNING
}
